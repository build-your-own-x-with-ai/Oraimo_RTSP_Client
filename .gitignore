# macOS
.DS_Store

# Xcode 用户态：窗口布局、断点、scheme 排序，都是本机数据，
# 跟着提交只会在多人协作时反复冲突。
xcuserdata/
*.xcuserstate

# 构建产物
build/
DerivedData/
*.ipa
*.dSYM.zip

# Swift Package Manager，以及 Tests/.build/ 里的测试驱动二进制、
# 服务器日志、对照实验的补丁副本 —— 全是可重新生成的产物。
# 无前导斜杠的模式在任意层级匹配，所以这一行同时管住两处。
.build/
.swiftpm/

# Python 字节码。server.py 被当模块导入时就会生成（抽取脚本、场景脚本都会）。
__pycache__/
*.pyc

# 签名与密钥
*.p12
*.cer
*.mobileprovision
